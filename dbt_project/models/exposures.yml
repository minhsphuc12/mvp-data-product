version: 2

exposures:
  - name: branch_monthly_executive_view
    type: dashboard
    maturity: low
    url: "http://localhost:8080" # placeholder — wire to Metabase later if desired
    description: >
      Conceptual executive view of branch × month KPIs sourced from
      mart_branch_monthly_performance (loan volume, repayments, policies, claims, cross-sell).
    depends_on:
      - ref('mart_branch_monthly_performance')
    owner:
      name: Finance demo
      email: demo@example.com
